#![allow(unused)]

use combine::parser;

fn main() {
    // let test_str = "take a dump";
    // let test_parser = sep_by(word, parse::char::space());
    // let par_res = test_parser.parse(test_str);

    let test_str = "42,69";

    // let test_parser = sep_by();
}
